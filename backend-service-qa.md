# Qthing 后台服务（同步中转）讨论归档（QA）

> 目标：用最低的复杂度，实现“个人多设备数据流转”。  
> 原则：轻量、简单、好部署、可靠；不追求自动合并与实时一致。  
> 第一阶段默认：优先免服务器部署（Cloudflare Workers + D1 + R2）。

---

## 🎯 定位与边界

**Q1：这个后台服务到底是什么？**  
A1：它是一个“同步中转站”，只负责存储与分发“各设备分支的最新内容”，不负责成为权威数据源，也不负责把多端变更自动合成一个结果。

**Q2：后台服务要解决的核心问题是什么？**  
A2：让你在设备 A 写完后，设备 B 能看到；设备 B 写完后，设备 A 能看到。仅此而已。

**Q3：明确不做什么？（Non-goals）**  
A3：  
- 不做多人实时协作  
- 不做自动合并/自动冲突解决  
- 不做版本历史/回滚/审计（只保留每个分支的最新 Head）  
- 不做“云端编辑器”，云端只存储与分发  
- 不追求低延迟与高并发（个人、小范围设备数为主）  

**Q4：为什么要把服务做得这么“瘦”？**  
A4：因为同步最大的复杂度来自“离线并发编辑同一文件”的自动合并。Qthing 的同步理念是把复杂度留给用户的显式选择（查看/覆盖），而不是系统去做隐式融合。

---

## 🔁 同步模型（服务端要支持的语义）

**Q5：最终选定的同步模型是什么？**  
A5：每个文件都是一个“小仓库”，只有“设备分支”，每个分支只保留一个最新 Head：  
- 设备 = 分支  
- 一个文件在云端会同时存在多个设备分支的最新内容  
- 设备只能编辑并自动推送自己的分支  
- 其他设备分支内容只能查看；若要采用，必须手动覆盖到自己的分支  
- 云端不需要“active 指针”，因为查看者永远是某台设备，指向谁属于本地视图选择  

**Q6：为什么不需要云端 active 指针？**  
A6：因为不存在“非设备的第三方查看端”。每个查看动作都发生在某台设备上，设备可以自行决定当前参考哪个分支内容，不需要云端强行给出一个全局“主分支”。

**Q7：你说“我只需要上行推送”，那下行还需要吗？**  
A7：服务端仍然需要提供下行读取接口，否则其他设备无法“看到”。区别在于：  
- 上行：自动/半自动（按策略推送到“我的分支”）  
- 下行：只读拉取，用于查看/手动覆盖时取内容；绝不自动覆盖本地

**Q7.1：上行推送的同步策略怎么做才“轻量但不容易忘”？**  
A7.1：不需要“每字节同步”。建议提供多种策略，默认组合：  
- 定时同步：每 N 秒/分钟尝试推送一次（有队列就 flush）  
- 启动/回到前台同步：进入 App 或插件启动后 flush 一次  
- 手动同步：用户点一次“立即同步/发布”  
说明：客户端应做合并与节流（例如 debounce + 只保留队列最后一次内容），语义仍然是“云端以本机分支为准”。  

---

## 🧱 最小数据模型（概念）

**Q8：最小需要哪些实体？**  
A8：在“单用户 + 多设备”的前提下，最少两个概念即可（是否落库由实现决定）：  
- `device`：每台设备一个 `device_id`（客户端生成并持久化），可选显示名  
- `head`：`(file_key, device_id)` 对应的最新内容引用与元信息  

**Q9：什么是 file_key？**  
A9：文件在同步域内的稳定标识。最简可用：使用“规范化相对路径”。  

**Q10：Head 需要包含哪些字段？**  
A10（建议最小集合）：  
- `file_key`  
- `device_id`  
- `content_hash`（用于判断一致/不同）  
- `updated_at`（用于展示“哪个更新更晚”）  
- `blob_ref` 或 `content`（内容本体或指向存储的引用）  

---

## 🔌 最小接口（概念）

**Q11：服务端最少需要哪些 API？**  
A11：  
- 写入我的分支最新 Head（覆盖写）：`PUT /files/{file_key}/heads/{device_id}`  
- 列出该文件所有分支 Head（用于差异提示）：`GET /files/{file_key}/heads`  
- 拉取某分支内容（查看/覆盖前读取）：`GET /files/{file_key}/heads/{device_id}`  

---

## 🔐 认证与权限（单用户优先，尽量轻但要可靠）

**Q12：我们是否要做多用户？**  
A12：第一阶段不做。只做单用户 + 多设备。

**Q13：认证最简方案是什么？**  
A13：用 token 即可，不做账号系统：
- 服务端维护一份“设备白名单”（device_id → device_name + device_token）
- 客户端请求携带 `X-Device-Id` 与 `Authorization: Bearer <device_token>`（或 `X-Device-Token`）
- 服务端只允许“白名单设备”读写自己的分支；不在白名单的一律拒绝

**Q14：设备标识怎么生成与管理？**  
A14：最简：首次启动生成 UUID 作为 `device_id` 并持久化；用户可选给设备起名用于 UI 展示。服务端侧的白名单建议用环境变量/secret 配置：
- `DEVICE_A_ID=...`、`DEVICE_A_NAME=phone`、`DEVICE_A_TOKEN=...`
- `DEVICE_B_ID=...`、`DEVICE_B_NAME=desktop`、`DEVICE_B_TOKEN=...`
说明：这套方案避免引入数据库层的“账号/注册/登录”，部署最轻。

---

## 🗄️ 平台与存储选型：优先免服务器（Cloudflare）

**Q15：为什么把 Cloudflare 作为第一阶段主线？**  
A15：因为很多人没有稳定服务器，也不会为了同步中转专门买 VPS。Cloudflare 提供“免运维的运行环境”，更符合“轻量、好部署”的目标。

**Q16：Cloudflare 上用什么组合最合适？KV / D1 / R2 怎么选？**  
A16：推荐 Workers + D1 + R2：
- Workers：跑 HTTP API 与鉴权逻辑
- D1：存 head 元信息索引（`file_key + device_id -> hash/updated_at/r2_key/size`）
- R2：存内容本体（Markdown 文件），避免把大内容塞进 KV
说明：Workers KV 更适合缓存/配置类数据；用它承载频繁写入的“内容与索引”会更别扭。

**Q17：Cloudflare 免费额度是否够用？**  
A17：对“单用户 + 少量设备 + 非字节级推送”的场景通常够用：
- Workers Free：每天 100,000 次请求，单次调用含 10ms CPU
- D1 Free：每天 5,000,000 行读取、100,000 行写入；总存储 5GB
- R2 Free：每月 10GB-month 存储；每月 1,000,000 次 Class A（写）与 10,000,000 次 Class B（读）；对公网 egress 免费
注意：免费额度按日/按月重置，政策可能变化；仍建议保持“可迁移”的数据结构。

**Q17.1：免费额度大概能用到什么程度？（粗略估算）**  
A17.1：可以用“每天文件更新次数”来估算。假设一次“文件更新”对应：
- 1 次 Workers 请求
- 1 次 D1 写入（更新该文件该设备分支的 head 记录）
- 1 次 R2 写入（上传该版本内容）

那么免费额度的上限大致是：
- Workers：≤ 100,000 次更新/天
- D1 写入：≤ 100,000 行写入/天
- R2 写入：≤ 1,000,000 次写入/月（约 ≤ 33,333 次/天）

对个人使用的直觉换算（示例）：
- 轻量：每天改 50 个文件，每个文件同步 2 次 → 100 次更新/天（远低于上限）
- 常规：每天改 200 个文件，每个文件同步 5 次 → 1,000 次更新/天（远低于上限）
- 偏重：每天改 1,000 个文件，每个文件同步 10 次 → 10,000 次更新/天（仍在安全区间）

存储额度（R2 10GB-month）的直觉换算（示例）：
- 10KB/条笔记：约 1,000,000 条
- 100KB/条笔记：约 100,000 条
说明：这里按“存量”估算，具体还取决于附件/图片是否纳入同步域。

**Q18：数据库怎么选才符合“轻量、好部署、可靠”？**  
A18：按优先级排序：
- 第一阶段主线：Cloudflare Workers + D1 + R2（免服务器，部署门槛最低）
- 自建备选：SQLite（依赖少且事务更稳），或单服务 + 文件系统 KV（最少依赖但要处理原子写）

---

## 🧰 部署形态（追求“好部署”）

**Q19：什么形态最符合“轻量、简单、好部署、可靠”？**  
A19：优先 Cloudflare（免服务器），其次才是自建 Docker：  
- 路线 0（第一阶段主线）：Cloudflare Workers + D1 + R2（只要域名/Cloudflare 账号即可）  
- 路线 A（自建替代）：单个服务容器 + 本地挂载卷（文件系统存储）  
- 路线 B（自建替代）：服务容器 + SQLite（数据库文件也挂载卷）  
说明：路线 A/B 都能做到“一条 docker compose 启动 + 备份一个目录/一个文件”。

---

## ✅ 可靠性与风险（仍需明确）

**Q20：在“极简后端”里，哪些可靠性点必须认真对待？**  
A20：  
- 写入原子性：避免半写入导致内容损坏（先写临时文件再原子替换）  
- 内容一致性：`content_hash` 以内容本体计算，作为“是否一致/是否变化”的唯一依据  
- 断网重试：客户端推送需要队列与重试（服务端保持幂等：同 hash 的重复写入可直接覆盖或忽略）  
- 备份策略：定期打包 `data/` 或 SQLite 文件即可  

**Q21：什么时候需要提升复杂度（比如引入更强的数据库/对象存储）？**  
A21：当你真的出现这些需求再加：  
- 文件数量/体积大到需要流式上传与对象存储  
- 未来要支持多用户，需要更强的权限、审计与配额  
- 想做历史版本与回滚（会引入“版本管理”的复杂度）  

---

## 🧪 最小可用闭环（用于验收服务是否“落地”）

**Q22：后台服务的 MVP 成功标准是什么？**  
A22：  
- 设备 A 能把某个文件持续推送为“我的分支 Head”  
- 设备 B 能列出该文件的所有分支，并读取设备 A 的 Head 内容用于查看  
- 设备 B 能手动把设备 A 的内容覆盖到“设备 B 的分支”，并能再被设备 A 看到  
- 期间不发生任何自动合并或自动覆盖本地内容  

